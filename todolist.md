Osten版本 0.1.4-alpha | 2026-09-06

---

一、核心禁令（单条可辩护版本）

Osten 的最终决策不得由“关键词 / 规则 / 检索命中”直接产生。规则、检索、编码器只能作为外围工具、上下文来源或安全护栏。核心输出必须由固定规模神经网络基于当前状态、目标、记忆上下文与心跳信号实时前向生成。

二、替代方案清单

被替代 替代方案 约束
关键词匹配 → 预设回应 输入必须编码为状态向量；禁止直接对原始字符串查表应答 编码器可学习或固定，但不做决策
规则引擎 允许硬安全护栏用最小规则；决策主链必须由网络前向产生 规则仅限“边界保护”，不参与核心推理
检索式回答 记忆检索 = 上下文供给；最终动作/文本/状态变更由网络解码 检索不“挑选答案”，只供给证据/状态
Transformer 明确候选架构：SSM / 状态空间 / 小 CNN / TCN / SNN / 世界模型 / 策略网络；禁止 self-attention 与 next-token 最终实现可由开发者从中选或自研
词预测 核心决策不做 next-token；文本若存在，只能作为动作参数的受控解码，不承载核心推理 文本不是核心证明
LLM 固定规模、本地推理、外部训练、热更新权重；不暗示可无限变强 规模上限 ≤ 8 GB

三、输入表征（硬点）

文本输入必须经编码器转为向量：

方案 说明
固定编码器（frozen） 如字符 n-gram 哈希、预训练 small embedding（外部加载）
可学习编码器 随在线学习更新，但参数量计入 8 GB 上限
禁止 直接将原始字符串作为网络输入

编码器只做信息转换，不做决策。

四、输出本体（硬点）

默认输出 说明
状态变更 修改当前状态向量
动作指令 执行某个操作
记忆写入 将当前经验存入记忆系统
内部计划 更新目标或策略
文本输出（可选） 仅作为动作参数的受控解码结果，不承载核心推理

文本输出不是 Osten 的核心证明。能在无输入时持续输出状态变更，就是运转证明。

五、训练策略（硬点）

策略 说明
状态预测 学习状态转换的规律
动作价值 学习不同行动带来的结果
好奇心 / 内在奖励 探索新的状态空间
进化策略 在外部演化中优化权重
Offline RL 从外部数据进行策略学习
混合 以上任意组合

训练目标不是“预测更准”，而是“行动更优”。

六、评测任务（硬点）

基线 说明
存活时间 能持续运行多久而不崩溃或陷入循环
输出多样性 同样输入在状态变化后输出是否不同
记忆利用率 输出中是否体现记忆内容
目标达成率 是否完成自己设定的短期目标
自一致性 输出是否与自身状态描述一致
行动连续性 输出是否前后关联（不随机跳跃）

七、一句话总结

Osten 是一个固定规模、非 Transformer、非 next-token 的本地决策引擎。核心输出由神经网络前向生成，文本（若有）只是动作参数的受控解码结果，不承载核心推理。